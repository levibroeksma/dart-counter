---
interface Props {
  name?: string;
  nameExpr?: string;
  isBot?: boolean;
  isBotExpr?: string;
  isGuest?: boolean;
}

const {
  name = "You",
  nameExpr,
  isBot = false,
  isBotExpr,
} = Astro.props;
import ProfileIcon from "@icons/profile.svg";
import BotIcon from "@icons/bot.svg";
---

<div class="flex items-center flex-col gap-2">
  <div class="relative flex p-2 items-center justify-center rounded-full border border-border bg-popover text-muted-foreground">
    {
      isBotExpr ? (
        <>
          <BotIcon class="size-8" aria-hidden="true" x-show={isBotExpr} x-cloak />
          <ProfileIcon
            class="size-8"
            aria-hidden="true"
            x-show={`!(${isBotExpr})`}
            x-cloak
          />
        </>
      ) : isBot ? (
        <BotIcon class="size-8" aria-hidden="true" />
      ) : (
        <ProfileIcon class="size-8" aria-hidden="true" />
      )
    }
    <span
      class="absolute top-12 left-1/2 -translate-x-1/2 -translate-y-1/2 rounded-full border border-border bg-popover px-1.5 py-0.5 text-center text-xs font-medium text-foreground"
      {...(nameExpr ? { "x-text": nameExpr } : {})}
    >
      {nameExpr ? "" : name}
    </span>
  </div>
</div>
