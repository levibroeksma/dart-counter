---
import type { GameType } from "@lib/shared/games/types";

interface Props {
  game: GameType;
}
---

<main class="mx-auto w-full max-w-2xl flex-1 p-4 @sm:p-6  flex flex-col">
  <!-- <header class="mb-6 space-y-1">
    <h1 class="font-display text-3xl uppercase tracking-wide text-foreground">{game.displayName}</h1>
  </header> -->
  <slot />
</main>
